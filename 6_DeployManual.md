# Setup manual
## System Requirements
- OS (Windows, Linux, macOS, etc.)
- Hardware Requirements

## Requirements
- Programming language version
- Package managers
- Framework dependencies
- Database or external services

## Setup Instructions
Provide commands and steps to run the project locally, e. g.
- **Clone project**: git clone my_repository_url
- **Install dependencies**: pip install -r requirements.txt
- **Setup virtual environment** (if applies): python -m venv .venv
- **Set environment variables**: (sample file)
> .env
> MY_SECRET_DATA=OOOOOOOOO
> API_KEY=NOTHXYOU
- **Run locally**: python manage.py runserver
- **Run tests** (optional): python manage.py test